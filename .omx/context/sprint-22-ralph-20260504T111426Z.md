# Context Snapshot: Sprint 22 Ralph Persistent Execution Loop

## Task statement
Implement `.claude/contracts/sprint-22-contract.md`: user-activated `$ralph` persistent execution loop in the harness.

## Desired outcome
Hooks enforce BUILD-only `$ralph` activation, turn-packet loop guidance, verifier-gated completion, protected ralph state, iteration accounting, evidence checkpoint bridge, and all listed acceptance criteria.

## Known facts/evidence
- Contract path: `.claude/contracts/sprint-22-contract.md`.
- Primary targets expected: `.claude/hooks/on-prompt-submit.sh`, `.claude/hooks/pre-write-gate.sh`, `.claude/hooks/pre-bash-gate.sh`, tests/simulations.
- `on-prompt-submit.sh` must start reading stdin JSON via `PROMPT_INPUT=$(cat)`.
- Gate scripts already read stdin into `INPUT_DATA`.

## Constraints
- Preserve existing hook behaviors.
- Do not change existing functions in `lib-helpers.sh`.
- No new dependencies.
- Windows/MSYS gotchas: use `tr '\\' '/'`, strip CR from jq, robust read loops.
- Ralph writes use existing `atomic_write`.
- Verify changed shell files with `bash -n` and simulate criteria.

## Unknowns/open questions
- Exact existing hook architecture and test harness shape.
- Existing state/evidence checkpoint helpers and current sprint discovery.
- Existing strategy loop STUCK behavior to mirror.

## Likely codebase touchpoints
- `.claude/hooks/on-prompt-submit.sh`
- `.claude/hooks/pre-write-gate.sh`
- `.claude/hooks/pre-bash-gate.sh`
- `.claude/hooks/lib-helpers.sh` (read-only/reference only)
- Existing tests under repo test directories.